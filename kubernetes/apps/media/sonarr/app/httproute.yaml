---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sonarr
spec:
  parentRefs:
    - name: internal
      namespace: network
      sectionName: websecure
  hostnames:
    - "sonarr.${SECRET_DOMAIN}"
  rules:
    - backendRefs:
        - name: sonarr
          port: 8989
