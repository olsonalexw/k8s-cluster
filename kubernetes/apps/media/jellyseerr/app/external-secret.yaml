---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: seerr-db
  namespace: media
spec:
  refreshInterval: 1m  # Sync every minute
  secretStoreRef:
    kind: ClusterSecretStore
    name: crunchy-pgo-secrets
  target:
    name: seerr-db-secret
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: postgres-pguser-seerr # Name of kubernetes secret in database namespace
        property: password
    - secretKey: host
      remoteRef:
        key: postgres-pguser-seerr
        property: host
    - secretKey: port
      remoteRef:
        key: postgres-pguser-seerr
        property: port
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: seerr-tls-ca
  namespace: media
spec:
  refreshInterval: 1m
  secretStoreRef:
    kind: ClusterSecretStore
    name: crunchy-pgo-secrets
  target:
    name: seerr-tls-ca-secret
    creationPolicy: Owner
  data:
    - secretKey: ca.crt
      remoteRef:
        key: pgo-root-cacert
        property: root.crt
