---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: unpackerr
  namespace: media
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  dataFrom:
    - extract:
        key: media/unpackerr
